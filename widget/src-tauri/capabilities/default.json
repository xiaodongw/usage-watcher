{
  "identifier": "default",
  "description": "What the panel window is allowed to ask the Rust side for. Tauri v2 denies everything not listed here, so a plugin that is registered but not granted fails silently at runtime.",
  "windows": ["panel"],
  "permissions": ["core:default", "notification:default"]
}
